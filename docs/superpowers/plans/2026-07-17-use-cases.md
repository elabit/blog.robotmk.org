# Use-Cases-Sektion — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Eine neue Hauptmenü-Kategorie „Use Cases" mit Dropdown, sechs Unterseiten und einer Übersichtsseite, die zeigt, welche Probleme Robot Framework löst.

**Architecture:** Hugo Page Bundles unter `content/use-cases/`, gerendert durch zwei sektionseigene Layouts (`single.html`, `list.html`). Struktur kommt aus dem Front Matter, Prosa aus dem Markdown-Body — dadurch ist einheitliches Aussehen erzwungen, nicht Disziplinsache. Die Navigation bekommt ein Dropdown an zwei Render-Pfaden (Desktop-Leiste, Mobile-Overlay).

**Tech Stack:** Hugo 0.124.1 extended, Go-Templates, CSS Custom Properties (`assets/css/tokens.css`), ES-Module (`js.Build`), keine Fremdbibliotheken.

**Spec:** `docs/superpowers/specs/2026-07-17-use-cases-design.md`

## Global Constraints

- **Branch:** `worktree-usecases`, bereits auf `why-robotmk` rebased. Nicht auf `main` arbeiten — dort fehlt das Design-System.
- **Keine Hard-Coded-Farben oder -Abstände.** Ausschließlich Tokens aus `assets/css/tokens.css` (`--color-*`, `--space-*`, `--text-*`, `--transition-*`). Beide Themes müssen funktionieren: Dark ist Default (`:root`), Light ab `tokens.css:115`.
- **Kein Syntax-Highlighting für `.robot`-Code.** Chroma hat keinen RobotFramework-Lexer (empirisch geprüft). Fence-Sprache ist ` ```robot ` wie im Blog; sie färbt nichts, benennt aber `data-lang`.
- **Kein neues JS-Modul.** Dropdown-Logik gehört in `assets/js/nav.js`, das bereits `export function init()` hat und in `main.js` registriert ist.
- **BEM-Namensschema** wie im Bestand: `.use-case__hero`, `.use-case__proof`. Sections tragen `<section class="X section">` + `<div class="container">`.
- **Zweisprachig, immer beide.** Jede Content-Datei existiert als `.de.md` **und** `.en.md`. Menü-Einträge in `hugo.yaml` pro Sprache.
- **Theme-Submodule nicht anfassen.** `themes/mainroad` ist leer und bleibt leer — `theme:` steht nicht mehr in `hugo.yaml`, der Build braucht es nicht.
- **`layouts/partials/menu.html` und `header.html` sind toter Code.** Nicht anfassen, nicht als Vorbild nehmen. Gerendert wird `nav.html` (via `baseof.html:35`).

## Verifikations-Konvention

Dieses Repo hat kein Test-Framework. „Test" heißt hier: bauen und die gerenderte HTML prüfen. Überall gilt:

```bash
BUILD=/tmp/uc-build
rm -rf "$BUILD" && hugo --quiet -d "$BUILD"
```

Ein Build ohne Ausgabe ist ein erfolgreicher Build. Assertions danach mit `grep` auf die erzeugten Dateien.

---

### Task 1: Hero-Motiv verallgemeinern und Beams-Leichen entfernen

Das isometrische Gitter steckt in einem `why-`-benannten Partial. Use Cases sollen es
mitbenutzen, ohne es zu kopieren. Gleichzeitig fliegen die Reste der entfernten grünen
Strahlen raus.

**Files:**
- Create: `layouts/partials/hero-motif.html` (aus `why-hero-motif.html`)
- Create: `assets/css/components/hero-motif.css`
- Delete: `layouts/partials/why-hero-motif.html`
- Modify: `layouts/why-robotmk/list.html:6`
- Modify: `assets/css/sections/why-robotmk.css:31-50`
- Modify: `layouts/partials/head/css.html`

**Interfaces:**
- Produces: Partial `hero-motif.html`, aufrufbar als `{{ partial "hero-motif.html" . }}`. Rendert ein `<svg class="hero-motif">` mit `<g class="hero-motif__grid">`. Erwartet einen Eltern-Container mit `position: relative` und `overflow: hidden`; legt sich per `position:absolute; inset:0` darüber, `z-index: 0`.
- Produces: CSS-Klassen `.hero-motif`, `.hero-motif__grid`.

- [ ] **Step 1: Referenz-Snapshot der Why-Seite ziehen (das ist der Test)**

Die Why-Seite muss nach dem Umbau **pixelgleiche HTML** liefern — bis auf die Klassennamen.
Erst den Ist-Zustand festhalten:

```bash
BUILD=/tmp/uc-build
rm -rf "$BUILD" && hugo --quiet -d "$BUILD"
grep -o 'class="why-hero__motif[a-z-]*"' "$BUILD/de/why-robotmk/index.html" | sort | uniq -c
grep -c '<line ' "$BUILD/de/why-robotmk/index.html"
```

Erwartet: `why-hero__motif` und `why-hero__motif-grid` je einmal, und **55** `<line>`-Elemente.
Diese Zahl ist die Referenz — sie darf sich nicht ändern.

- [ ] **Step 2: Partial umbenennen und Klassen umbenennen**

```bash
git mv layouts/partials/why-hero-motif.html layouts/partials/hero-motif.html
```

Dann in `layouts/partials/hero-motif.html`:

- Kopf-Kommentar (Zeilen 1–6) ersetzen durch:

```
{{- /*
  Subtle isometric background motif. Two families of 2:1-isometric lattice
  lines (currentColor, theme-aware via CSS) echo the homepage tile world.
  Purely decorative and static. Shared by the Why Robotmk and Use Cases heroes.

  Requires a parent with position:relative and overflow:hidden.
*/ -}}
```

- Den kompletten `<defs>`-Block (Zeilen 8–14) **löschen**. Der `why-beam`-Gradient wird
  seit der Entfernung der grünen Strahlen von nichts mehr referenziert.
- `class="why-hero__motif"` → `class="hero-motif"` (im `<svg>`-Tag)
- `class="why-hero__motif-grid"` → `class="hero-motif__grid"` (im `<g>`-Tag)
- Die beiden langen `<line>`-Zeilen **unverändert lassen**.

- [ ] **Step 3: CSS in eine eigene Komponente ziehen**

Create `assets/css/components/hero-motif.css`:

```css
/* ============================================================
   Hero Motif — shared isometric lattice backdrop
   Used by the Why Robotmk and Use Cases heroes.
   Parent must be position:relative + overflow:hidden.
   ============================================================ */

.hero-motif {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
  pointer-events: none;
  color: var(--color-on-surface-variant);
}

.hero-motif__grid {
  stroke-opacity: 0.08;
}
```

- [ ] **Step 4: Alte Regeln aus why-robotmk.css entfernen**

In `assets/css/sections/why-robotmk.css` die drei Regeln `.why-hero__motif`,
`.why-hero__motif-grid` und `.why-hero__motif-beams` (Zeilen ~31–50, inklusive des
Kommentars darüber, der noch „teal beams" behauptet) **löschen**. `.why-hero` selbst mit
`position: relative` und `overflow: hidden` **bleibt** — der Eltern-Container wird
gebraucht.

- [ ] **Step 5: Aufruf in der Why-Seite umstellen**

`layouts/why-robotmk/list.html:6`:

```html
    {{ partial "hero-motif.html" . }}
```

- [ ] **Step 6: Neue CSS-Datei einhängen**

In `layouts/partials/head/css.html` eine Zeile bei den Komponenten ergänzen und in die
Concat-Slice aufnehmen:

```
{{- $heromotif := resources.Get "css/components/hero-motif.css" -}}
```

und in der `slice` **vor** `$hero` einsetzen:

```
{{- $css := slice $tokens $reset $base $buttons $cards $badges $forms $nav $toolsticker $heromotif $hero $homepage $pages $whyRobotmk $blog
```

- [ ] **Step 7: Test — Why-Seite unverändert, nur neue Klassen**

```bash
BUILD=/tmp/uc-build
rm -rf "$BUILD" && hugo --quiet -d "$BUILD"
echo "--- alte Klassen muessen weg sein (erwartet: 0) ---"
grep -c 'why-hero__motif' "$BUILD/de/why-robotmk/index.html" || echo 0
echo "--- neue Klassen muessen da sein (erwartet: je 1) ---"
grep -o 'class="hero-motif[a-z_-]*"' "$BUILD/de/why-robotmk/index.html" | sort | uniq -c
echo "--- Gitter unveraendert (erwartet: 55) ---"
grep -c '<line ' "$BUILD/de/why-robotmk/index.html"
echo "--- toter Gradient muss weg sein (erwartet: 0) ---"
grep -c 'why-beam' "$BUILD/de/why-robotmk/index.html" || echo 0
```

Erwartet: alte Klassen 0, `hero-motif` und `hero-motif__grid` je 1, **55** Zeilen, `why-beam` 0.

- [ ] **Step 8: Commit**

```bash
git add layouts/partials/hero-motif.html assets/css/components/hero-motif.css \
        assets/css/sections/why-robotmk.css layouts/why-robotmk/list.html \
        layouts/partials/head/css.html
git rm --cached layouts/partials/why-hero-motif.html 2>/dev/null || true
git add -A layouts/partials/
git commit -m "Generalise the hero motif into a shared partial

The isometric lattice was named after the Why Robotmk page but is not
specific to it — the Use Cases heroes reuse it as-is.

Drops the why-beam gradient and the .why-hero__motif-beams rule, both
dead since the teal beams were removed, and the comment that still
described them."
```

---

### Task 2: Citrix-Seite end-to-end

Eine vollständige Use-Case-Seite als vertikaler Schnitt: Content-Bundle, Layout, CSS.
Die anderen fünf folgen danach demselben Muster.

**Files:**
- Create: `content/use-cases/_index.de.md`, `content/use-cases/_index.en.md`
- Create: `content/use-cases/citrix/index.de.md`, `content/use-cases/citrix/index.en.md`
- Create: `layouts/use-cases/single.html`
- Create: `assets/css/sections/use-cases.css`
- Modify: `layouts/partials/head/css.html`

**Interfaces:**
- Consumes: `hero-motif.html` aus Task 1.
- Produces: Front-Matter-Vertrag für alle Use-Case-Seiten:
  - `problem` (string) — der H1
  - `teaser` (string) — ein bis zwei Sätze unter dem H1
  - `libraries` (list of strings) — Badge-Liste
  - `proof.gif` (string, optional) — Pfad relativ zum Bundle
  - `proof.contrast` (string, optional) — Pfad relativ zum Bundle
  - `proof.alt` (string) — Alt-Text für das gezeigte Asset
  - `weight` (int) — Reihenfolge in Menü und Übersicht
- Produces: CSS-Klassen `.use-case__hero`, `.use-case__eyebrow`, `.use-case__title`, `.use-case__teaser`, `.use-case__libs`, `.use-case__lib`, `.use-case__proof`, `.use-case__proof-media`, `.use-case__proof-caption`, `.use-case__body`, `.use-case__cta`.

- [ ] **Step 1: Assertions schreiben (der Test)**

Diese Datei ist Wegwerf-Werkzeug, nicht Teil des Repos:

```bash
cat > /tmp/assert-citrix.sh <<'SH'
#!/bin/bash
B=/tmp/uc-build
F="$B/de/use-cases/citrix/index.html"
fail=0
check() { if eval "$2"; then echo "  OK   $1"; else echo "  FAIL $1"; fail=1; fi; }

echo "== Citrix-Seite =="
check "Seite existiert"            "[ -f '$F' ]"
check "H1 = Problem-Satz"          "grep -q 'Die Filiale kommt morgens nicht rein' '$F'"
check "Eyebrow vorhanden"          "grep -q 'use-case__eyebrow' '$F'"
check "Hero-Motiv eingebunden"     "grep -q 'hero-motif' '$F'"
check "Beweis-Slot da"             "grep -q 'use-case__proof' '$F'"
check "Library-Badges da"          "grep -q 'use-case__lib' '$F'"
check "Browser als Library"        "grep -q '>Browser<' '$F'"
check "ImageHorizon als Library"   "grep -q '>ImageHorizon<' '$F'"
check "robot-Codeblock da"         "grep -q 'data-lang=\"robot\"' '$F'"
check "CTA da"                     "grep -q 'use-case__cta' '$F'"
check "EN-Version existiert"       "[ -f '$B/en/use-cases/citrix/index.html' ]"
exit $fail
SH
chmod +x /tmp/assert-citrix.sh
```

- [ ] **Step 2: Test laufen lassen — muss fehlschlagen**

```bash
rm -rf /tmp/uc-build && hugo --quiet -d /tmp/uc-build
/tmp/assert-citrix.sh
```

Erwartet: `FAIL Seite existiert` und alle folgenden — die Sektion gibt es noch nicht.

- [ ] **Step 3: Sektions-Index anlegen**

Create `content/use-cases/_index.de.md`:

```markdown
---
title: "Use Cases"
description: "Welche Probleme Robot Framework löst — von Web-Anwendungen über Citrix und SAP bis zu REST-APIs. Eine Sprache für jede Technologie."
---
```

Create `content/use-cases/_index.en.md`:

```markdown
---
title: "Use Cases"
description: "The problems Robot Framework solves — from web applications through Citrix and SAP to REST APIs. One language for every technology."
---
```

- [ ] **Step 4: Citrix-Content anlegen**

Create `content/use-cases/citrix/index.de.md`:

````markdown
---
title: "Citrix"
weight: 20
problem: "Die Filiale kommt morgens nicht rein."
teaser: "Die Session startet, aber die veröffentlichte Anwendung hängt im Splash-Screen. Checkmk sagt: alle Server grün."
libraries: ["Browser", "ImageHorizon"]
proof:
  gif: "img/citrix.gif"
  alt: "Ein Robot-Framework-Test meldet sich an der Citrix-StoreFront an, startet die veröffentlichte Anwendung und erkennt deren Oberfläche per Bildvergleich."
description: "Citrix-Verfügbarkeit überwachen, wie ein Anwender sie erlebt: Anmeldung an der StoreFront, Start der veröffentlichten Anwendung, Bilderkennung im Session-Fenster."
---

## Warum es weh tut

Ihr Monitoring sieht die Citrix-Infrastruktur von außen: Die Delivery Controller antworten,
die VDAs sind registriert, die Lizenzserver haben Luft, die StoreFront liefert HTTP 200.
Jeder einzelne Wert ist grün.

Trotzdem steht die Filiale. Denn was der Anwender erlebt, passiert **innerhalb** der
Session — und dort sieht kein Agent hin. Die veröffentlichte Anwendung wartet auf ein
Netzlaufwerk, das nicht mountet. Ein Profil lädt nicht. Ein Splash-Screen bleibt stehen,
weil eine Lizenzabfrage in ein Timeout läuft.

Die Session selbst ist technisch einwandfrei. Nur benutzen kann sie niemand.

Das ist die teuerste Sorte Ausfall: Sie erfahren davon nicht aus dem Monitoring, sondern
vom Telefon.

## Wie Robot Framework es löst

Der Test macht, was ein Mitarbeiter morgens macht — und zwar alle paar Minuten, bevor der
erste Mitarbeiter es versucht.

Die **Browser Library** meldet sich an der StoreFront an. Das ist normale Web-Automation:
Formularfelder, Klicks, Wartebedingungen. Ab dem Moment, in dem die `.ica`-Datei den
Citrix-Client startet, endet die Zuständigkeit des Browsers — ab hier gibt es kein DOM
mehr, nur noch Pixel.

Dort übernimmt die **ImageHorizonLibrary**. Sie erkennt Oberflächenelemente per
Bildvergleich, genau wie ein Mensch: Sie wartet, bis das Anwendungsfenster erscheint,
klickt in das richtige Feld, tippt, und prüft, ob am Ende der erwartete Bildschirm da ist.

Damit misst der Test nicht die Infrastruktur, sondern die Erfahrung: *Kommt die Filiale
rein — ja oder nein?*

```robot
*** Settings ***
Library    Browser
Library    ImageHorizonLibrary    reference_folder=${CURDIR}/img

*** Test Cases ***
Filiale kann Warenwirtschaft starten
    New Browser    chromium    headless=${False}
    New Page    ${STOREFRONT_URL}
    Fill Text    id=username    ${USER}
    Fill Text    id=password    ${PASSWORD}
    Click    text=Anmelden
    Click    text=Warenwirtschaft

    # Ab hier gibt es kein DOM mehr — nur noch Pixel.
    Wait For    app_window.png    timeout=60
    Click Image    kundensuche.png
    Type    ${KUNDENNUMMER}
    Wait For    kundendatensatz.png    timeout=20
```
````

Create `content/use-cases/citrix/index.en.md`:

````markdown
---
title: "Citrix"
weight: 20
problem: "The branch office can't get in this morning."
teaser: "The session starts, but the published application hangs on its splash screen. Checkmk says every server is green."
libraries: ["Browser", "ImageHorizon"]
proof:
  gif: "img/citrix.gif"
  alt: "A Robot Framework test signs in to Citrix StoreFront, launches the published application and recognises its interface by image comparison."
description: "Monitor Citrix availability the way a user experiences it: sign in to StoreFront, launch the published application, recognise the session window by image."
---

## Why it hurts

Your monitoring sees the Citrix infrastructure from the outside: delivery controllers
respond, VDAs are registered, license servers have headroom, StoreFront returns HTTP 200.
Every single value is green.

The branch office is still stuck. Because what the user experiences happens **inside** the
session — and no agent looks there. The published application waits on a network drive
that won't mount. A profile fails to load. A splash screen sits there because a license
lookup ran into a timeout.

The session itself is technically flawless. It just can't be used.

That's the most expensive kind of outage: you don't hear about it from your monitoring,
you hear about it from the phone.

## How Robot Framework solves it

The test does what an employee does in the morning — every few minutes, before the first
employee tries.

The **Browser Library** signs in to StoreFront. That's ordinary web automation: form
fields, clicks, wait conditions. The moment the `.ica` file hands over to the Citrix
client, the browser's job ends — from here there is no DOM, only pixels.

That's where **ImageHorizonLibrary** takes over. It recognises interface elements by image
comparison, exactly like a human does: it waits for the application window to appear,
clicks the right field, types, and checks that the expected screen is there at the end.

So the test doesn't measure infrastructure. It measures the experience: *can the branch
office get in — yes or no?*

```robot
*** Settings ***
Library    Browser
Library    ImageHorizonLibrary    reference_folder=${CURDIR}/img

*** Test Cases ***
Branch Office Can Start Inventory System
    New Browser    chromium    headless=${False}
    New Page    ${STOREFRONT_URL}
    Fill Text    id=username    ${USER}
    Fill Text    id=password    ${PASSWORD}
    Click    text=Log On
    Click    text=Inventory System

    # From here there is no DOM — only pixels.
    Wait For    app_window.png    timeout=60
    Click Image    customer_search.png
    Type    ${CUSTOMER_ID}
    Wait For    customer_record.png    timeout=20
```
````

- [ ] **Step 5: Layout schreiben**

Create `layouts/use-cases/single.html`:

```html
{{ define "title" }}{{ .Params.problem | default .Title }} — {{ .Site.Title }}{{ end }}

{{ define "main" }}
<section class="use-case__hero section">
  {{ partial "hero-motif.html" . }}
  <div class="container">
    <span class="use-case__eyebrow">{{ T "use_case_eyebrow" }} &middot; {{ .Title }}</span>
    <h1 class="use-case__title">{{ .Params.problem }}</h1>
    <p class="use-case__teaser">{{ .Params.teaser }}</p>
    {{- with .Params.libraries }}
    <ul class="use-case__libs" role="list">
      {{- range . }}
      <li class="use-case__lib">{{ . }}</li>
      {{- end }}
    </ul>
    {{- end }}
  </div>
</section>

<section class="use-case__proof section section--surface">
  <div class="container">
    {{- /* GIF wins where one exists; the contrast view is the universal fallback.
           Pages must never render an empty proof slot.
           Guarded with `with`: a page without a proof block must warn, not
           crash the build on a nil lookup. */ -}}
    {{- with .Params.proof }}
      {{- $media := or .gif .contrast -}}
      {{- $alt := .alt -}}
      {{- with $media }}
        {{- with $.Resources.GetMatch . }}
        <figure class="use-case__proof-media">
          <img src="{{ .RelPermalink }}" alt="{{ $alt }}" loading="lazy"
               {{ with .Width }}width="{{ . }}"{{ end }}
               {{ with .Height }}height="{{ . }}"{{ end }}>
        </figure>
        {{- else }}
        {{- warnf "use-case %q: proof asset %q not found in bundle" $.File.Path $media }}
        {{- end }}
      {{- else }}
      {{- warnf "use-case %q: proof block has neither gif nor contrast" $.File.Path }}
      {{- end }}
    {{- else }}
    {{- warnf "use-case %q: no proof block in front matter" $.File.Path }}
    {{- end }}
  </div>
</section>

<section class="use-case__body section">
  <div class="container">
    <div class="use-case__prose">
      {{ .Content }}
    </div>
  </div>
</section>

<section class="use-case__cta section section--surface">
  <div class="container">
    <p class="use-case__cta-text">{{ T "use_case_cta_text" }}</p>
    <a class="btn btn--primary" href="{{ "/services/" | relLangURL }}">{{ T "use_case_cta_button" }}</a>
  </div>
</section>
{{ end }}
```

- [ ] **Step 6: i18n-Strings ergänzen**

An `i18n/de.yaml` anhängen:

```yaml
use_case_eyebrow:
  other: "Use Case"
use_case_cta_text:
  other: "Sie wollen das in Ihrer Umgebung laufen sehen?"
use_case_cta_button:
  other: "Clarity Call buchen →"
```

An `i18n/en.yaml` anhängen:

```yaml
use_case_eyebrow:
  other: "Use Case"
use_case_cta_text:
  other: "Want to see this running in your environment?"
use_case_cta_button:
  other: "Book a Clarity Call →"
```

- [ ] **Step 7: CSS schreiben**

Create `assets/css/sections/use-cases.css`:

```css
/* ============================================================
   Use Cases — subpage and overview
   ============================================================ */

/* --- Hero --- */
.use-case__hero {
  position: relative;   /* anchors .hero-motif */
  overflow: hidden;     /* the motif must never cause horizontal scroll */
  text-align: center;
}

.use-case__hero .container {
  position: relative;
  z-index: 1;
}

.use-case__eyebrow {
  display: block;
  font-size: var(--text-label-size);
  font-weight: var(--text-label-weight);
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--color-primary);
  margin-bottom: var(--space-sm);
}

.use-case__title {
  font-family: var(--font-display);
  font-size: var(--text-display-size);
  line-height: 1.1;
  color: var(--color-on-surface);
  max-width: 18ch;
  margin-inline: auto;
}

.use-case__teaser {
  font-size: var(--text-body-lg-size);
  color: var(--color-on-surface-variant);
  max-width: 60ch;
  margin: var(--space-md) auto 0;
}

/* --- Library badges --- */
.use-case__libs {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: var(--space-sm);
  list-style: none;
  margin: var(--space-lg) 0 0;
  padding: 0;
}

.use-case__lib {
  font-family: var(--font-mono);
  font-size: var(--text-label-size);
  color: var(--color-on-surface-variant);
  background-color: var(--color-surface);
  border: 1px solid var(--color-surface-border);
  border-radius: 999px;
  padding: var(--space-2xs) var(--space-sm);
}

/* --- Proof slot --- */
.use-case__proof-media {
  margin: 0;
  border: 1px solid var(--color-surface-border);
  border-radius: var(--radius-md);
  overflow: hidden;
  background-color: var(--color-surface-low);
}

.use-case__proof-media img {
  display: block;
  width: 100%;
  height: auto;
}

/* --- Prose body --- */
.use-case__prose {
  max-width: 68ch;
  margin-inline: auto;
  color: var(--color-on-surface-variant);
}

.use-case__prose h2 {
  font-family: var(--font-display);
  font-size: var(--text-headline-size);
  color: var(--color-on-surface);
  margin-top: var(--space-2xl);
}

.use-case__prose h2:first-child {
  margin-top: 0;
}

/* Robot Framework code: deliberately not syntax-highlighted.
   Chroma has no RobotFramework lexer, so these blocks are plain text —
   they earn their keep through typography, not colour. */
.use-case__prose pre {
  background-color: var(--color-surface-low);
  border: 1px solid var(--color-surface-border);
  border-left: 3px solid var(--color-primary);
  border-radius: var(--radius-md);
  padding: var(--space-md);
  overflow-x: auto;   /* long keyword lines must scroll, not break the page */
}

.use-case__prose pre code {
  font-family: var(--font-mono);
  font-size: 14px;
  line-height: 1.7;
  color: var(--color-on-surface);
  white-space: pre;
}

/* --- CTA --- */
.use-case__cta {
  text-align: center;
}

.use-case__cta-text {
  font-family: var(--font-display);
  font-size: var(--text-headline-size);
  color: var(--color-on-surface);
  margin-bottom: var(--space-lg);
}

@media (max-width: 768px) {
  .use-case__title {
    max-width: none;
  }
}
```

**Vor dem Schreiben prüfen:** `--text-display-size`, `--text-body-lg-size`,
`--text-headline-size`, `--text-label-size`, `--font-mono`, `--radius-md`, `--space-2xs`
müssen in `tokens.css` existieren:

```bash
for t in --text-display-size --text-body-lg-size --text-headline-size --text-label-size --text-label-weight --font-mono --font-display --radius-md --space-2xs --space-sm --space-md --space-lg --space-2xl; do
  grep -q -- "$t:" assets/css/tokens.css && echo "OK   $t" || echo "FEHLT $t"
done
```

Jedes `FEHLT` muss durch das nächstliegende vorhandene Token ersetzt werden — **kein
neues Token erfinden und keinen Zahlenwert hart eintragen.**

- [ ] **Step 8: CSS einhängen**

In `layouts/partials/head/css.html`:

```
{{- $usecases := resources.Get "css/sections/use-cases.css" -}}
```

und in die Slice **nach** `$whyRobotmk`:

```
{{- $css := slice $tokens $reset $base $buttons $cards $badges $forms $nav $toolsticker $heromotif $hero $homepage $pages $whyRobotmk $usecases $blog
```

- [ ] **Step 9: Platzhalter-GIF ablegen**

Das echte GIF nimmt Simon auf. Damit der Build nicht warnt und das Layout prüfbar ist,
ein Platzhalter gleicher Abmessung:

```bash
mkdir -p content/use-cases/citrix/img
cp content/blog/rcc-ultimate-tool/img/windows-rcc.gif content/use-cases/citrix/img/citrix.gif
```

**In `TODO.md` vermerken:** `citrix.gif` ist ein Platzhalter und muss ersetzt werden.

- [ ] **Step 10: Test laufen lassen — muss durchlaufen**

```bash
rm -rf /tmp/uc-build && hugo --quiet -d /tmp/uc-build
/tmp/assert-citrix.sh
```

Erwartet: alle Zeilen `OK`, Exit-Code 0.

- [ ] **Step 11: Beide Themes und Mobil sichten**

```bash
hugo server -D
```

Öffnen: `http://localhost:1313/de/use-cases/citrix/`

Prüfen und nur weitergehen, wenn alles zutrifft:
- Dark **und** Light Mode lesbar (Betriebssystem-Theme umschalten)
- Gitter-Motiv im Hero sichtbar, aber dezent
- Kein horizontaler Scroll bei 375px Breite
- Der `.robot`-Block scrollt horizontal statt die Seite zu sprengen

- [ ] **Step 12: Commit**

```bash
git add content/use-cases layouts/use-cases assets/css/sections/use-cases.css \
        layouts/partials/head/css.html i18n/de.yaml i18n/en.yaml
git commit -m "Add the Use Cases section with the Citrix page

Establishes the pattern the remaining five pages follow: structure comes
from front matter, prose from the markdown body, so a page cannot
accidentally look different from its neighbours.

The proof slot falls back from GIF to contrast view in the layout rather
than in each page, which is what lets the pages that cannot be filmed
still look complete.

Robot Framework code is deliberately unhighlighted — Chroma has no
RobotFramework lexer."
```

---

### Task 3: Die fünf weiteren Use-Case-Seiten

**Files:**
- Create: `content/use-cases/{web,sap-gui,windows-desktop,rest-api,saas}/index.{de,en}.md`

**Interfaces:**
- Consumes: Front-Matter-Vertrag und `layouts/use-cases/single.html` aus Task 2. Keine Layout- oder CSS-Änderung — wenn eine Seite ein neues Feld bräuchte, ist das ein Signal, dass der Vertrag falsch ist. Dann erst den Vertrag ändern, nicht die Seite ausscheren lassen.

Jede Seite: `weight` steuert die Reihenfolge, `problem` ist der H1, Body hat genau zwei
`##`-Überschriften („Warum es weh tut" / „Wie Robot Framework es löst") plus einen
` ```robot `-Block.

**Zuordnung (aus der Spec):**

| Slug | weight | Problem (DE) | libraries | proof |
|---|---|---|---|---|
| `web` | 10 | „Der Checkout bricht ab — und keiner merkt es." | `["Browser"]` | `gif` |
| `citrix` | 20 | (Task 2) | | |
| `sap-gui` | 30 | „Die Transaktion hängt. Alle Server sind grün." | `["SapGui"]` | `gif` |
| `windows-desktop` | 40 | „Der Client von 2009, den keiner ablöst." | `["ImageHorizon", "PlatynUI"]` | `gif` |
| `rest-api` | 50 | „200 OK heißt nicht, dass der Auftrag angelegt wurde." | `["Requests"]` | `contrast` |
| `saas` | 60 | „Die Statuspage sagt grün. Deine Leute sagen was anderes." | `["Browser"]` | `contrast` |

- [ ] **Step 1: Assertions für alle sechs schreiben**

```bash
cat > /tmp/assert-all.sh <<'SH'
#!/bin/bash
B=/tmp/uc-build
fail=0
for slug in web citrix sap-gui windows-desktop rest-api saas; do
  for lang in de en; do
    F="$B/$lang/use-cases/$slug/index.html"
    if [ ! -f "$F" ]; then echo "  FAIL $lang/$slug fehlt"; fail=1; continue; fi
    for cls in use-case__title use-case__proof use-case__lib use-case__cta hero-motif; do
      grep -q "$cls" "$F" || { echo "  FAIL $lang/$slug: $cls fehlt"; fail=1; }
    done
    grep -q 'data-lang="robot"' "$F" || { echo "  FAIL $lang/$slug: kein robot-Codeblock"; fail=1; }
    # Beweis-Slot darf nie leer sein
    grep -q 'use-case__proof-media' "$F" || { echo "  FAIL $lang/$slug: Beweis-Slot leer"; fail=1; }
    [ $fail -eq 0 ] && echo "  OK   $lang/$slug"
  done
done
exit $fail
SH
chmod +x /tmp/assert-all.sh
```

- [ ] **Step 2: Test laufen lassen — fünf Seiten müssen fehlen**

```bash
rm -rf /tmp/uc-build && hugo --quiet -d /tmp/uc-build && /tmp/assert-all.sh
```

Erwartet: `citrix` OK, die anderen fünf `FAIL ... fehlt`.

- [ ] **Step 3: Die fünf Bundles anlegen**

**Was dieser Plan hier bewusst nicht liefert — und warum:** Die Prosa dieser fünf Seiten
steht nicht im Plan. Nicht aus Nachlässigkeit: Sie lebt von Fachwissen über SAP-Landschaften,
PlatynUI und Checkmks HTTP-Checks, das ein Agent nicht hat. Plausibel klingender,
fachlich falscher Marketing-Text wäre schlechter als gar keiner. Der Plan liefert deshalb
den vollständigen, verbindlichen Front-Matter-Vertrag pro Seite und die inhaltlichen
Leitplanken; die Prosa entsteht beim Umsetzen entlang des Citrix-Vorbilds aus Task 2
Step 4 und **muss von Simon gegengelesen werden**, bevor sie live geht.

**Front Matter, verbindlich — DE:**

```yaml
# content/use-cases/web/index.de.md
title: "Web"
weight: 10
problem: "Der Checkout bricht ab — und keiner merkt es."
teaser: "Alle Komponenten antworten, die Zertifikate sind gültig, die Datenbank hat Luft. Nur bezahlen kann niemand."
libraries: ["Browser"]
proof:
  gif: "img/web.gif"
  alt: "Ein Robot-Framework-Test legt einen Artikel in den Warenkorb und durchläuft den Checkout bis zur Bestellbestätigung."
```

```yaml
# content/use-cases/sap-gui/index.de.md
title: "SAP GUI"
weight: 30
problem: "Die Transaktion hängt. Alle Server sind grün."
teaser: "Der Applikationsserver antwortet, die Arbeitsprozesse laufen, die Datenbank ist gesund. Der Sachbearbeiter wartet trotzdem."
libraries: ["SapGui"]
proof:
  gif: "img/sap-gui.gif"
  alt: "Ein Robot-Framework-Test meldet sich am SAP GUI an, ruft eine Transaktion auf und prüft das Ergebnis im Dynpro."
```

```yaml
# content/use-cases/windows-desktop/index.de.md
title: "Windows Desktop"
weight: 40
problem: "Der Client von 2009, den keiner ablöst."
teaser: "Kein Agent sieht hinein, kein Hersteller supportet ihn, und das halbe Haus arbeitet damit."
libraries: ["ImageHorizon", "PlatynUI"]
proof:
  gif: "img/windows-desktop.gif"
  alt: "Ein Robot-Framework-Test bedient eine Windows-Fat-Client-Anwendung — einmal per Bildvergleich, einmal über den UI-Baum."
```

```yaml
# content/use-cases/rest-api/index.de.md
title: "REST API"
weight: 50
problem: "200 OK heißt nicht, dass der Auftrag angelegt wurde."
teaser: "Ein Statuscode beweist, dass der Server antwortet. Nicht, dass Ihr Geschäftsprozess funktioniert."
libraries: ["Requests"]
proof:
  contrast: "img/rest-api-contrast.png"
  alt: "Links die Checkmk-Services der API-Hosts, alle grün. Rechts der Robot-Framework-Test, der die mehrstufige Transaktion prüft und fehlschlägt."
```

```yaml
# content/use-cases/saas/index.de.md
title: "SaaS"
weight: 60
problem: "Die Statuspage sagt grün. Deine Leute sagen was anderes."
teaser: "Sie zahlen dafür, Sie können keinen Agenten installieren, und die Statusseite des Anbieters ist Marketing."
libraries: ["Browser"]
proof:
  contrast: "img/saas-contrast.png"
  alt: "Links die Checkmk-Services rund um den SaaS-Zugang, alle grün. Rechts der Robot-Framework-Test, der die Anmeldung durchspielt und scheitert."
```

**EN:** Dieselben Slugs und `weight`s, `title` und `libraries` identisch, `problem`/`teaser`/`alt`
übersetzt. Die englischen Problem-Sätze aus der Spec-Tabelle übernehmen.

**Body-Struktur, für alle fünf gleich** (wie Citrix in Task 2 Step 4): genau zwei
`##`-Überschriften — „Warum es weh tut" / „Wie Robot Framework es löst" (EN: „Why it hurts" /
„How Robot Framework solves it") — und danach ein ` ```robot `-Block mit einem echten,
lauffähigen Testfall.

**Inhaltliche Leitplanken, die den Wert der Seiten ausmachen:**

- **`rest-api`** muss offensiv beantworten, *warum überhaupt* Robot Framework: Checkmk kann
  HTTP-Checks längst selbst. Der Text muss klarmachen, dass es nicht um „URL antwortet mit
  200" geht, sondern um die mehrstufige Transaktion mit Zustand — Token holen, Auftrag
  anlegen, Status pollen, Ergebnis verifizieren, aufräumen. Ohne diese Abgrenzung fragt der
  Checkmk-Admin zu Recht „wozu?".
- **`windows-desktop`** stellt zwei Ansätze gegenüber statt einen zu verkaufen:
  ImageHorizon (Bildvergleich — sieht, was der Mensch sieht, aber pixelabhängig) gegen
  PlatynUI (Zugriff auf den UI-Baum — robust, Robot-Framework-first). Der Vergleich *ist*
  der Inhalt.
- **`saas`** argumentiert über das, was du nicht kontrollierst: kein Agent installierbar,
  die Statuspage des Anbieters ist Marketing, du zahlst trotzdem.
- **`web`** ist die Einstiegsseite für die meisten Leser — hier muss der Text am
  konkretesten sein.
- **`sap-gui`** ist der Lead-Magnet für den DACH-Markt.

**Platzhalter-Assets** wie in Task 2 Step 9 ablegen: `web`, `sap-gui`, `windows-desktop`
bekommen ein `img/<slug>.gif`; `rest-api` und `saas` ein `img/<slug>-contrast.png`
(Kontrast-Sicht) mit `proof.contrast` statt `proof.gif`. Jeden Platzhalter in `TODO.md`
eintragen.

- [ ] **Step 4: Test laufen lassen — alle zwölf müssen durchlaufen**

```bash
rm -rf /tmp/uc-build && hugo --quiet -d /tmp/uc-build && /tmp/assert-all.sh
```

Erwartet: 12 Zeilen `OK`, Exit-Code 0.

- [ ] **Step 5: Prüfen, dass keine Hugo-Warnung fällt**

Das Layout warnt per `warnf`, wenn ein Beweis-Asset fehlt:

```bash
rm -rf /tmp/uc-build && hugo -d /tmp/uc-build 2>&1 | grep -i "warn" || echo "keine Warnungen"
```

Erwartet: `keine Warnungen`.

- [ ] **Step 6: Commit**

```bash
git add content/use-cases TODO.md
git commit -m "Add the remaining five use case pages

Every page fills the same front-matter contract, so none of them needed
a layout or CSS change — which is the point of the pattern.

Proof assets are placeholders; TODO.md tracks which ones Simon still has
to record."
```

---

### Task 4: Übersichtsseite mit Karten-Grid

**Files:**
- Create: `layouts/use-cases/list.html`
- Modify: `assets/css/sections/use-cases.css`

**Interfaces:**
- Consumes: Front-Matter-Vertrag aus Task 2. Die Karten ziehen `problem`, `libraries` und `.Title` aus `.Pages` — die Übersicht darf **keine** eigene Kopie dieser Texte halten, sonst laufen Übersicht und Subpage auseinander.
- Produces: CSS-Klassen `.use-cases__grid`, `.use-cases__card`, `.use-cases__card-title`, `.use-cases__card-problem`, `.use-cases__card-libs`.

- [ ] **Step 1: Assertions schreiben**

```bash
cat > /tmp/assert-overview.sh <<'SH'
#!/bin/bash
B=/tmp/uc-build
F="$B/de/use-cases/index.html"
fail=0
check() { if eval "$2"; then echo "  OK   $1"; else echo "  FAIL $1"; fail=1; fi; }

check "Uebersicht existiert"        "[ -f '$F' ]"
check "Grid vorhanden"              "grep -q 'use-cases__grid' '$F'"
check "sechs Karten"                "[ \$(grep -o 'use-cases__card\"' '$F' | wc -l) -eq 6 ]"
check "Citrix-Problem als Aufhaenger" "grep -q 'Die Filiale kommt morgens nicht rein' '$F'"
check "SAP-Problem da"              "grep -q 'Die Transaktion haengt\|Die Transaktion hängt' '$F'"
check "Link auf Citrix-Subpage"     "grep -q 'href=\"/de/use-cases/citrix/\"' '$F'"
check "EN-Uebersicht existiert"     "[ -f '$B/en/use-cases/index.html' ]"
exit $fail
SH
chmod +x /tmp/assert-overview.sh
```

- [ ] **Step 2: Test laufen lassen — muss fehlschlagen**

```bash
rm -rf /tmp/uc-build && hugo --quiet -d /tmp/uc-build && /tmp/assert-overview.sh
```

Erwartet: `FAIL Grid vorhanden` — die Seite existiert zwar (Hugo erzeugt sie automatisch),
nutzt aber das Standard-Listenlayout.

- [ ] **Step 3: list.html schreiben**

Create `layouts/use-cases/list.html`:

```html
{{ define "title" }}{{ .Title }} — {{ .Site.Title }}{{ end }}

{{ define "main" }}
<section class="use-cases__hero section">
  {{ partial "hero-motif.html" . }}
  <div class="container">
    <span class="use-case__eyebrow">{{ T "use_case_eyebrow" }}</span>
    <h1 class="use-case__title">{{ T "use_cases_overview_title" }}</h1>
    <p class="use-case__teaser">{{ T "use_cases_overview_teaser" }}</p>
  </div>
</section>

<section class="use-cases__list section section--surface">
  <div class="container">
    <ul class="use-cases__grid" role="list">
      {{- range .Pages.ByWeight }}
      <li>
        <a class="use-cases__card" href="{{ .RelPermalink }}">
          <span class="use-cases__card-title">{{ .Title }}</span>
          {{- /* The problem sentence is pulled from the page itself, never copied,
                 so the overview cannot drift from the subpage. */ -}}
          <span class="use-cases__card-problem">{{ .Params.problem }}</span>
          {{- with .Params.libraries }}
          <span class="use-cases__card-libs">
            {{- range . }}<span class="use-case__lib">{{ . }}</span>{{ end -}}
          </span>
          {{- end }}
        </a>
      </li>
      {{- end }}
    </ul>
  </div>
</section>
{{ end }}
```

- [ ] **Step 4: i18n ergänzen**

`i18n/de.yaml`:

```yaml
use_cases_overview_title:
  other: "Eine Sprache. Jede Technologie."
use_cases_overview_teaser:
  other: "Web, Citrix, SAP, Desktop, REST, SaaS — Robot Framework testet sie alle. Ihr Team lernt eine Sprache statt fünf Werkzeuge."
```

`i18n/en.yaml`:

```yaml
use_cases_overview_title:
  other: "One language. Every technology."
use_cases_overview_teaser:
  other: "Web, Citrix, SAP, desktop, REST, SaaS — Robot Framework tests them all. Your team learns one language instead of five tools."
```

- [ ] **Step 5: Karten-CSS ergänzen**

An `assets/css/sections/use-cases.css` anhängen:

```css
/* --- Overview --- */
.use-cases__hero {
  position: relative;
  overflow: hidden;
  text-align: center;
}

.use-cases__hero .container {
  position: relative;
  z-index: 1;
}

.use-cases__grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: var(--space-lg);
  list-style: none;
  margin: 0;
  padding: 0;
}

.use-cases__card {
  display: flex;
  flex-direction: column;
  gap: var(--space-sm);
  height: 100%;
  padding: var(--space-lg);
  background-color: var(--color-surface);
  border: 1px solid var(--color-surface-border);
  border-radius: var(--radius-md);
  text-decoration: none;
  transition: border-color var(--transition-fast), transform var(--transition-fast);
}

.use-cases__card:hover {
  border-color: var(--color-primary);
  transform: translateY(-2px);
}

.use-cases__card-title {
  font-family: var(--font-display);
  font-size: var(--text-label-size);
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--color-primary);
}

.use-cases__card-problem {
  font-family: var(--font-display);
  font-size: var(--text-title-size);
  line-height: 1.3;
  color: var(--color-on-surface);
}

.use-cases__card-libs {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-2xs);
  margin-top: auto;   /* badges sit at the card foot regardless of text length */
  padding-top: var(--space-md);
}

@media (prefers-reduced-motion: reduce) {
  .use-cases__card {
    transition: none;
  }
  .use-cases__card:hover {
    transform: none;
  }
}
```

Token-Prüfung wie in Task 2 Step 7 für `--text-title-size` und `--radius-md`.

- [ ] **Step 6: Test laufen lassen**

```bash
rm -rf /tmp/uc-build && hugo --quiet -d /tmp/uc-build && /tmp/assert-overview.sh
```

Erwartet: alle `OK`.

- [ ] **Step 7: Commit**

```bash
git add layouts/use-cases/list.html assets/css/sections/use-cases.css i18n/de.yaml i18n/en.yaml
git commit -m "Add the Use Cases overview as a card grid

The cards read problem and libraries from each page's front matter
rather than keeping their own copy, so the overview cannot drift from
the subpages it advertises.

This page carries the breadth argument — one language for every
technology — which no single use case page can make on its own."
```

---

### Task 5: Desktop-Dropdown

**Files:**
- Modify: `hugo.yaml` (Menü beider Sprachen)
- Modify: `layouts/partials/nav.html:14-22`
- Modify: `assets/css/components/nav.css`

**Interfaces:**
- Consumes: URLs `/de/use-cases/<slug>/` und `/en/use-cases/<slug>/` aus Task 2/3.
- Produces: Markup-Vertrag `<li class="nav__item nav__item--has-children">` mit `<button class="nav__dropdown-toggle" aria-expanded="false" aria-controls="...">` und `<ul class="nav__dropdown" id="...">`. Task 6 baut auf denselben Klassennamen im Overlay auf.

- [ ] **Step 1: Assertions schreiben**

```bash
cat > /tmp/assert-nav.sh <<'SH'
#!/bin/bash
B=/tmp/uc-build
F="$B/de/index.html"
fail=0
check() { if eval "$2"; then echo "  OK   $1"; else echo "  FAIL $1"; fail=1; fi; }

check "Use-Cases-Eintrag im Menue"  "grep -q 'Use Cases' '$F'"
check "Dropdown-Container da"       "grep -q 'nav__dropdown' '$F'"
check "has-children-Marker"         "grep -q 'nav__item--has-children' '$F'"
check "Toggle ist ein button"       "grep -q '<button class=\"nav__dropdown-toggle' '$F'"
# grep ist zeilenweise, das button-Tag umfasst mehrere Zeilen -> mit tr zu einer Zeile machen
check "aria-expanded am Toggle"     "tr -d '\n' < '$F' | grep -q 'nav__dropdown-toggle[^>]*aria-expanded=\"false\"'"
check "aria-controls am Toggle"     "tr -d '\n' < '$F' | grep -q 'nav__dropdown-toggle[^>]*aria-controls='"
check "sechs Kindeintraege"         "[ \$(grep -o 'nav__dropdown-link' '$F' | wc -l) -ge 6 ]"
check "Citrix im Dropdown"          "grep -q '/de/use-cases/citrix/' '$F'"
check "Services weiter da"          "grep -q '/de/services/' '$F'"
exit $fail
SH
chmod +x /tmp/assert-nav.sh
```

- [ ] **Step 2: Test laufen lassen — muss fehlschlagen**

```bash
rm -rf /tmp/uc-build && hugo --quiet -d /tmp/uc-build && /tmp/assert-nav.sh
```

Erwartet: alles außer „Services weiter da" schlägt fehl.

- [ ] **Step 3: Menü in hugo.yaml ergänzen und Gewichte umnumerieren**

**Achtung:** `Use Cases` bekommt Gewicht 4, wo aktuell `Services` steht. Ohne
Umnumerierung kollidieren beide und Hugo sortiert nach Name.

Im `en`-Block: `Services` auf `weight: 5`, `Blog` auf `weight: 6` ändern, dann einfügen:

```yaml
        - identifier: usecases
          name: "Use Cases"
          url: "/en/use-cases/"
          weight: 4
        - name: "Web"
          parent: usecases
          url: "/en/use-cases/web/"
          weight: 10
        - name: "Citrix"
          parent: usecases
          url: "/en/use-cases/citrix/"
          weight: 20
        - name: "SAP GUI"
          parent: usecases
          url: "/en/use-cases/sap-gui/"
          weight: 30
        - name: "Windows Desktop"
          parent: usecases
          url: "/en/use-cases/windows-desktop/"
          weight: 40
        - name: "REST API"
          parent: usecases
          url: "/en/use-cases/rest-api/"
          weight: 50
        - name: "SaaS"
          parent: usecases
          url: "/en/use-cases/saas/"
          weight: 60
```

Im `de`-Block dasselbe mit `/de/`-URLs, `Services` auf 5 und `Blog` auf 6.

- [ ] **Step 4: nav.html — Desktop-Zweig umbauen**

`layouts/partials/nav.html`, Zeilen 13–22 ersetzen:

```html
    {{- /* Desktop Nav Links */ -}}
    <ul class="nav__links" role="list">
      {{- $currentPage := . -}}
      {{- range .Site.Menus.main }}
      {{- if .HasChildren }}
      <li class="nav__item nav__item--has-children">
        {{- /* A button, not a link: the toggle must not navigate, and nav.js
               closes the mobile overlay on every <a> click. */ -}}
        <button class="nav__dropdown-toggle{{ if $currentPage.HasMenuCurrent "main" . }} nav__link--active{{ end }}"
                aria-expanded="false"
                aria-controls="dropdown-{{ .Identifier }}">
          {{ .Name }}
          <span class="nav__dropdown-caret" aria-hidden="true"></span>
        </button>
        <ul class="nav__dropdown" id="dropdown-{{ .Identifier }}" role="list">
          {{- range .Children }}
          <li>
            <a class="nav__dropdown-link{{ if $currentPage.IsMenuCurrent "main" . }} nav__link--active{{ end }}"
               href="{{ .URL }}">{{ .Name }}</a>
          </li>
          {{- end }}
        </ul>
      </li>
      {{- else }}
      <li class="nav__item">
        <a class="nav__link{{ if $currentPage.IsMenuCurrent "main" . }} nav__link--active{{ end }}"
           href="{{ .URL }}">{{ .Name }}</a>
      </li>
      {{- end }}
      {{- end }}
    </ul>
```

- [ ] **Step 5: Dropdown-CSS ergänzen**

An `assets/css/components/nav.css` **vor** dem Mobile-Breakpoint-Block anhängen:

```css
/* --- Desktop dropdown --- */
.nav__item--has-children {
  position: relative;
}

.nav__dropdown-toggle {
  display: inline-flex;
  align-items: center;
  gap: var(--space-2xs);
  font-family: var(--font-body);
  font-size: 15px;
  font-weight: 400;
  color: var(--color-on-surface-variant);
  background: none;
  border: none;
  border-bottom: 2px solid transparent;
  padding: 0 0 2px;
  cursor: pointer;
  transition: color var(--transition-fast), border-color var(--transition-fast);
}

.nav__dropdown-toggle:hover,
.nav__dropdown-toggle.nav__link--active {
  color: var(--color-on-surface);
}

.nav__dropdown-caret {
  width: 0;
  height: 0;
  border-left: 4px solid transparent;
  border-right: 4px solid transparent;
  border-top: 4px solid currentColor;
  transition: transform var(--transition-fast);
}

.nav__dropdown-toggle[aria-expanded="true"] .nav__dropdown-caret {
  transform: rotate(180deg);
}

.nav__dropdown {
  position: absolute;
  top: calc(100% + var(--space-sm));
  left: 0;
  min-width: 200px;
  display: none;
  flex-direction: column;
  gap: var(--space-xs);
  list-style: none;
  margin: 0;
  padding: var(--space-sm);
  background-color: var(--color-surface);
  border: 1px solid var(--color-surface-border);
  border-radius: var(--radius-md);
  z-index: var(--nav-z-index);
}

/* Opens on hover AND on click/keyboard — hover alone is unusable by keyboard. */
.nav__item--has-children:hover .nav__dropdown,
.nav__item--has-children:focus-within .nav__dropdown,
.nav__dropdown-toggle[aria-expanded="true"] + .nav__dropdown {
  display: flex;
}

.nav__dropdown-link {
  display: block;
  font-family: var(--font-body);
  font-size: 15px;
  color: var(--color-on-surface-variant);
  text-decoration: none;
  padding: var(--space-2xs) var(--space-xs);
  border-radius: var(--radius-sm);
  white-space: nowrap;
  transition: color var(--transition-fast), background-color var(--transition-fast);
}

.nav__dropdown-link:hover,
.nav__dropdown-link.nav__link--active {
  color: var(--color-on-surface);
  background-color: var(--color-surface-high);
}

@media (prefers-reduced-motion: reduce) {
  .nav__dropdown-toggle,
  .nav__dropdown-caret,
  .nav__dropdown-link {
    transition: none;
  }
}
```

Token-Prüfung für `--space-xs`, `--radius-sm`, `--font-body`, `--nav-z-index` wie gehabt.

- [ ] **Step 6: Toggle-Logik in nav.js ergänzen**

An `assets/js/nav.js` innerhalb von `init()` **vor** der schließenden Klammer anhängen:

```js
  // --- Desktop dropdown: click + keyboard. CSS handles hover on its own. ---
  document.querySelectorAll('.nav__dropdown-toggle').forEach(toggle => {
    toggle.addEventListener('click', (e) => {
      e.stopPropagation();
      const isOpen = toggle.getAttribute('aria-expanded') === 'true';
      // Only one dropdown open at a time.
      document.querySelectorAll('.nav__dropdown-toggle').forEach(t => {
        t.setAttribute('aria-expanded', 'false');
      });
      toggle.setAttribute('aria-expanded', String(!isOpen));
    });
  });

  // Click outside closes any open dropdown.
  document.addEventListener('click', () => {
    document.querySelectorAll('.nav__dropdown-toggle[aria-expanded="true"]').forEach(t => {
      t.setAttribute('aria-expanded', 'false');
    });
  });

  // Escape closes the dropdown and returns focus to its toggle.
  document.addEventListener('keydown', (e) => {
    if (e.key !== 'Escape') return;
    const open = document.querySelector('.nav__dropdown-toggle[aria-expanded="true"]');
    if (open) {
      open.setAttribute('aria-expanded', 'false');
      open.focus();
    }
  });
```

- [ ] **Step 7: Test laufen lassen**

```bash
rm -rf /tmp/uc-build && hugo --quiet -d /tmp/uc-build && /tmp/assert-nav.sh
```

Erwartet: alle `OK`.

- [ ] **Step 8: Von Hand prüfen — Maus und Tastatur**

```bash
hugo server -D
```

Auf `http://localhost:1313/de/` mit **Desktop-Breite**:
- Hover über „Use Cases" öffnet das Dropdown
- Klick auf „Use Cases" öffnet es ebenfalls und navigiert **nicht**
- **Nur mit Tab und Enter** erreichbar und bedienbar
- Escape schließt und der Fokus liegt wieder auf dem Toggle
- Klick daneben schließt
- Auf einer Use-Case-Seite ist „Use Cases" als aktiv markiert

- [ ] **Step 9: Commit**

```bash
git add hugo.yaml layouts/partials/nav.html assets/css/components/nav.css assets/js/nav.js
git commit -m "Add the Use Cases dropdown to the desktop nav

The toggle is a button rather than a link so it cannot navigate, and it
opens on click and keyboard as well as hover — hover alone locks out
keyboard users.

Renumbers Services and Blog, which collided with the new entry on
weight 4."
```

---

### Task 6: Mobile-Akkordeon im Overlay

Das Overlay zentriert seine Einträge auf Headline-Größe. Fünf Hauptpunkte plus sechs
Unterpunkte passen auf einem Telefon nicht — deshalb standardmäßig eingeklappt, und das
Overlay muss scrollen können.

**Files:**
- Modify: `layouts/partials/nav.html:64-80` (Overlay-Zweig)
- Modify: `assets/css/components/nav.css`
- Modify: `assets/js/nav.js`

**Interfaces:**
- Consumes: Menü-Struktur aus Task 5, Klassennamen `nav__dropdown-toggle` wiederverwendet.
- Produces: `.nav__overlay-toggle`, `.nav__overlay-sublinks`.

- [ ] **Step 1: Assertions schreiben**

```bash
cat > /tmp/assert-overlay.sh <<'SH'
#!/bin/bash
B=/tmp/uc-build
F="$B/de/index.html"
fail=0
check() { if eval "$2"; then echo "  OK   $1"; else echo "  FAIL $1"; fail=1; fi; }

check "Overlay-Toggle ist ein button" "grep -q '<button class=\"nav__overlay-toggle' '$F'"
check "Sublinks-Liste da"             "grep -q 'nav__overlay-sublinks' '$F'"
check "Citrix auch im Overlay"        "[ \$(grep -o '/de/use-cases/citrix/' '$F' | wc -l) -ge 2 ]"
check "Overlay-Toggle eingeklappt"    "grep -q 'nav__overlay-toggle[^>]*aria-expanded=\"false\"' '$F'"
exit $fail
SH
chmod +x /tmp/assert-overlay.sh
```

- [ ] **Step 2: Test laufen lassen — muss fehlschlagen**

```bash
rm -rf /tmp/uc-build && hugo --quiet -d /tmp/uc-build && /tmp/assert-overlay.sh
```

- [ ] **Step 3: Overlay-Zweig in nav.html umbauen**

Zeilen 64–80 ersetzen:

```html
  {{- /* Mobile Full-Screen Overlay */ -}}
  <div class="nav__overlay" id="nav-overlay" aria-hidden="true">
    <ul class="nav__overlay-links" role="list">
      {{- $currentPage := . -}}
      {{- range .Site.Menus.main }}
      {{- if .HasChildren }}
      <li>
        {{- /* Must be a button: nav.js closes the overlay on every <a> click,
               so a link here would collapse the menu instead of expanding it. */ -}}
        <button class="nav__overlay-toggle" aria-expanded="false"
                aria-controls="overlay-{{ .Identifier }}">
          {{ .Name }}
          <span class="nav__dropdown-caret" aria-hidden="true"></span>
        </button>
        <ul class="nav__overlay-sublinks" id="overlay-{{ .Identifier }}" role="list">
          {{- range .Children }}
          <li>
            <a class="nav__overlay-sublink{{ if $currentPage.IsMenuCurrent "main" . }} nav__link--active{{ end }}"
               href="{{ .URL }}">{{ .Name }}</a>
          </li>
          {{- end }}
        </ul>
      </li>
      {{- else }}
      <li>
        <a class="nav__overlay-link{{ if $currentPage.IsMenuCurrent "main" . }} nav__link--active{{ end }}"
           href="{{ .URL }}">{{ .Name }}</a>
      </li>
      {{- end }}
      {{- end }}
      <li>
        <a class="btn btn--primary nav__overlay-cta" href="{{ "/services/" | relLangURL }}">
          {{ T "nav_cta_mobile" | default "Clarity Call buchen &rarr;" | safeHTML }}
        </a>
      </li>
    </ul>
  </div>
```

**Hinweis:** Der bestehende Text „Clarity Call buchen →" ist im Original hart im Template.
Wenn `nav_cta_mobile` nicht in `i18n/` existiert, greift `default` — dann bleibt es beim
bisherigen Verhalten und nichts geht kaputt.

- [ ] **Step 4: Overlay-CSS ergänzen**

An `assets/css/components/nav.css` anhängen:

```css
/* --- Mobile accordion inside the overlay --- */
.nav__overlay-toggle {
  display: inline-flex;
  align-items: center;
  gap: var(--space-sm);
  font-family: var(--font-display);
  font-size: var(--text-headline-lg-size);
  font-weight: 600;
  color: var(--color-on-surface-variant);
  background: none;
  border: none;
  padding: 0;
  cursor: pointer;
}

.nav__overlay-toggle[aria-expanded="true"] {
  color: var(--color-on-surface);
}

.nav__overlay-sublinks {
  display: none;
  flex-direction: column;
  align-items: center;
  gap: var(--space-md);
  list-style: none;
  margin: var(--space-md) 0 0;
  padding: 0;
}

.nav__overlay-toggle[aria-expanded="true"] + .nav__overlay-sublinks {
  display: flex;
}

.nav__overlay-sublink {
  font-family: var(--font-body);
  font-size: var(--text-body-lg-size);
  color: var(--color-on-surface-variant);
  text-decoration: none;
}

.nav__overlay-sublink:hover,
.nav__overlay-sublink.nav__link--active {
  color: var(--color-primary);
}
```

Und die bestehende `.nav__overlay`-Regel um Scrollbarkeit ergänzen — mit sechs
aufgeklappten Unterpunkten passt die Liste sonst nicht auf ein Telefon:

```css
.nav__overlay {
  overflow-y: auto;
  justify-content: flex-start;
  padding-block: var(--space-2xl);
}
```

**Wichtig:** Das ist eine Änderung am Bestand. `justify-content: center` wird zu
`flex-start`, sonst wird bei Überlauf der obere Teil der Liste unerreichbar.

- [ ] **Step 5: Akkordeon-Logik in nav.js ergänzen**

Innerhalb von `init()` anhängen:

```js
  // --- Mobile accordion: independent of the desktop dropdown. ---
  document.querySelectorAll('.nav__overlay-toggle').forEach(toggle => {
    toggle.addEventListener('click', () => {
      const isOpen = toggle.getAttribute('aria-expanded') === 'true';
      toggle.setAttribute('aria-expanded', String(!isOpen));
    });
  });
```

**Nicht anfassen:** Die bestehende Schleife `overlay.querySelectorAll('a')` in Zeile 27
schließt das Overlay bei jedem Link-Klick. Weil die Toggles `<button>` sind, greift sie
dort nicht — genau so ist es beabsichtigt. Die Unterpunkte sind `<a>` und schließen das
Overlay beim Navigieren, was richtig ist.

- [ ] **Step 6: Test laufen lassen**

```bash
rm -rf /tmp/uc-build && hugo --quiet -d /tmp/uc-build && /tmp/assert-overlay.sh
```

- [ ] **Step 7: Von Hand prüfen — echtes Telefonformat**

```bash
hugo server -D
```

Browser auf **375×667** (iPhone SE) stellen:
- Hamburger öffnet das Overlay
- „Use Cases" klappt auf und zu, ohne das Overlay zu schließen
- Aufgeklappt sind **alle** Einträge erreichbar — notfalls durch Scrollen
- Ein Klick auf einen Unterpunkt navigiert **und** schließt das Overlay
- Escape schließt das Overlay
- Danach auf 768px und 1024px gegenprüfen, dass die Desktop-Leiste unverändert ist

- [ ] **Step 8: Commit**

```bash
git add layouts/partials/nav.html assets/css/components/nav.css assets/js/nav.js
git commit -m "Add the Use Cases accordion to the mobile overlay

A hover dropdown makes no sense on a phone, so the overlay gets a
collapsed accordion instead. The toggle is a button because nav.js
closes the overlay on every <a> click — a link would have collapsed the
menu instead of expanding it.

The overlay now scrolls and aligns to the top: eleven entries at
headline size do not fit a phone, and centring made the overflow
unreachable."
```

---

### Task 7: TODO.md nachziehen

**Files:**
- Modify: `TODO.md`

- [ ] **Step 1: Erledigten Eintrag streichen und Befunde festhalten**

Der Eintrag „responsive design: schmäler -> Menubar crappy @critical" bezieht sich auf das
tote `menu.html`. `nav.html` hat einen sauberen Hamburger mit Overlay — der Bug ist
faktisch erledigt.

In `TODO.md`:
- „responsive design: schmäler -> Menubar crappy" als erledigt markieren, mit dem Hinweis,
  dass die Ursache mit `nav.html` entfallen ist.
- Diese Punkte ergänzen:

```
Use Cases:
    ☐ GIF aufnehmen: web, citrix, sap-gui, windows-desktop @critical
    ☐ Kontrast-Sicht bauen: rest-api, saas (links Checkmk-Services, rechts RF-Test)
    ☐ Platzhalter-Assets in content/use-cases/*/img/ ersetzen @critical
    ☐ Phase 2: Mainframe/3270 — Screenshots mit geklärten Nutzungsrechten besorgen
    ☐ Problem-Sätze der sechs Seiten gegenlesen und schärfen

Code-Blöcke (Bestand, nicht Use Cases):
    ☐ Chroma hat keinen RobotFramework-Lexer — ```robot im Blog ist Klartext
    ☐ noClasses:true backt Monokai (#272822) als Inline-Style ein -> folgt keinem Theme-Token
    ☐ Entscheiden: markup.highlight.noClasses:false + Chroma-CSS an Tokens binden?

Tote Layouts:
    ☐ layouts/partials/menu.html und header.html werden nicht mehr gerendert -> löschen?
```

- [ ] **Step 2: Commit**

```bash
git add TODO.md
git commit -m "Update TODO after the Use Cases work

Closes the critical responsive-menubar item: it described menu.html,
which no longer renders.

Records what the Use Cases pages still need (real GIFs, contrast views)
and two pre-existing findings the work surfaced — Chroma has no
RobotFramework lexer, and Monokai is baked into every code block as an
inline style."
```

---

## Abschluss

- [ ] **Voller Build ohne Warnungen**

```bash
rm -rf /tmp/uc-build && hugo -d /tmp/uc-build 2>&1 | tail -15
```

Erwartet: Seitenzahl EN/DE um je 7 gestiegen (6 Use Cases + Übersicht), keine `WARN`-Zeile.

- [ ] **Alle Assertions noch einmal**

```bash
/tmp/assert-all.sh && /tmp/assert-overview.sh && /tmp/assert-nav.sh && /tmp/assert-overlay.sh
echo "Exit: $?"
```

- [ ] **Aufräumen**

```bash
rm -f /tmp/assert-*.sh
rm -rf /tmp/uc-build
```

- [ ] **REQUIRED SUB-SKILL:** `superpowers:requesting-code-review` vor dem Mergen.

## Offene Punkte für Simon

Diese Dinge kann kein Agent erledigen — sie brauchen dich:

1. **Echte GIFs aufnehmen** für web, citrix, sap-gui, windows-desktop. Bis dahin stehen Platzhalter drin, die in `TODO.md` vermerkt sind.
2. **Kontrast-Sichten bauen** für rest-api und saas (links Checkmk-Services, rechts der RF-Test).
3. **Problem-Sätze schärfen.** Sie sind mein Rohentwurf; du kennst deine Kunden.
4. **Die Prosa gegenlesen.** Besonders `rest-api` (Abgrenzung zu Checkmks HTTP-Checks) und `windows-desktop` (ImageHorizon vs. PlatynUI) — dort ist Fachwissen im Text, das ich nur zweiter Hand habe.
